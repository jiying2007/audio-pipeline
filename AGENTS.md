# audio-pipeline agent execution contract

Start at `docs/program/README.md`, then read `docs/program/PROCESS.md` and
`docs/program/plan.json`. The committed plan is the progress index; actual
GitHub checks/artifacts remain execution evidence. Re-read live main and open
PRs before writing. Do not repeat historical closed work from chat memory.

Current phase is software-public-data. Do not start product recording, DUT/HIL
or certification work. Preserve their deferred/blocked truth; do not mark PASS
or remove their future authority. Open-data research is allowed after license,
immutable source identity and exposure-role checks.

Pick the highest-priority dependency-ready task. Freeze exact base, hypothesis,
measurement contract, budgets and data roles before a search. Work on one
algorithm/root cause at a time. A measurement change and a shipping algorithm
change must not approve each other in one experiment. Preserve all failures.

Implement, run targeted/self/negative tests, use existing canonical evaluators,
then full exact-head CI. Never change acceptance thresholds to obtain green.
Freeze the candidate before independent confirmation; repeated data is replay,
not fresh independence. Respect confirmation budgets across branches/lineages.

Only merge after live expected head/base and all required applicable checks
are independently verified. Reverify the merged main. Release only through the
existing governed release chain when release-bearing. Clean source before final
verification; clean branches only after evidence archival and live-SHA/no-open-PR
checks. Never directly push main, force-update tags or delete unclassified refs.

Update the plan with evidence pointers after verification. Do not claim a task
CLOSED from a successful dispatch or research workflow alone. A code agent may
implement the next task during an active session; the repository scheduler only
executes registered bounded handlers and cannot invent missing implementations.
