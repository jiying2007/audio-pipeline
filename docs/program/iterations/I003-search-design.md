# I003 bounded selector search

This file freezes the interpretation of the I003 selector-search contract before any result is observed.

- Exact research base: `3094375309bc98f026068187d3d458d1017e34a0`.
- Shipping software baseline remains immutable `v2.3.12` / `d82cb6d2be76497d1d66dd16da00924411207046`.
- This phase evaluates exactly three pre-registered Sync route-selector architectures: `earliest-qualified`, `incumbent-qualified`, and `causal-cluster-leading-edge`.
- Development seeds `4107/4207` and validation seeds `9107/9207` are already-observed geometry-v2 evidence. They are selection/regression authority only and can never be independent confirmation.
- The run consumes inherited root-cause budget search round `2` and candidate variants `10..12`; confirmation consumption remains `0` even if no candidate is selected.
- Candidate definitions and constants are frozen before execution. Results may rank the three candidates but may not alter them or introduce a fourth candidate.
- A winner, if any, is frozen only for exact C implementation. It is not a shipping decision and cannot bypass engineering precheck, shadow protection, independent confirmation, hosted regression, exact-head Verify, main Verify, or release identity.
